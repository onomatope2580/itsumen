<div class="page">
  <div class="r-index">
    <div class="page-title-text"><%= @room.date %>　<%= @room.number %>部屋</div>
    <div class="back-page"><%= link_to "最初に戻る", root_path, class:"back" %></div>
  </div>

  <div class="members">
    <div class="side-tag"></div>
    <div class="member"><%= link_to "末吉", "#", class:"person" %></div>
    <div class="member"><%= link_to "田村", "#", class:"person" %></div>
    <div class="member"><%= link_to "濱田", "#", class:"person" %></div>
    <div class="member"><%= link_to "山口", "#", class:"person" %></div>
  </div>

  <div class="r-records">
    <%# ↓ここを繰り返す %>
    <% unless @sueyoshis.empty? %>
      <% @sueyoshis.each do |sueyoshi| %>
        <div class="r-record">
          <div class="r-top-bar">
            <div class="side-tag"><%= sueyoshi.turn %>半荘目</div>
            <div class="member point num"><%= sueyoshi.s_point %></div>
            <div class="member point num"><%= Tamura.find(sueyoshi.id).t_point %></div>
            <div class="member point num"><%= Hamada.find(sueyoshi.id).h_point %></div>
            <div class="member point num"><%= Yamaguchi.find(sueyoshi.id).y_point %></div>
          </div>
        </div>
      <% end %>
    <% end %>
    <%# ↑ここまで繰り返す %>
  </div>

  <div class="r-results">
    <div class="result total">
      <div class="side-tag">合計</div>
      <div class="member num"><%= @s_today_total %></div>
      <div class="member num"><%= @t_today_total %></div>
      <div class="member num"><%= @h_today_total %></div>
      <div class="member num"><%= @y_today_total %></div>
    </div>
  </div>

  <div class="add">
    <div class="add-record">
      <%= link_to "半荘の結果を入力",  new_room_record_path, id:"add-battle", class:"add-battle" %>
    </div>
  </div>
</div>